1. create getComputerChoice() which randomizes an answer out of 3 options.
2. create playRound(computerChoice, playerChoice) which will play a single round. Re-play the round on tie. *Case-insensitive choice string
3. create game() to play 5 rounds and decide the total winner. Display the results with console.log(), and receive input with prompt()
